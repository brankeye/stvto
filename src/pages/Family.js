import React from 'react';
import { Layout, Image, SEO, Typography } from 'components';

const Family = () => (
  <Layout>
    <SEO title="Family" />
    <Typography type={'title'}>Family</Typography>
    <Typography type={'body'}>Welcome to the Family page.</Typography>
  </Layout>
);

export default Family;
