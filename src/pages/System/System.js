import React from 'react';
import { Layout, Image, SEO, Typography } from 'components';

const System = () => (
  <Layout>
    <SEO title="Class Times" />
    <Typography type={'title'}>System</Typography>
    <Typography type={'body'}>Welcome to the System page.</Typography>
  </Layout>
);

export default System;
